<Page x:Class="Sudoku_WPF.InstructionsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="InstructionPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="8*" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Frame x:Name="InstructionFrame" NavigationUIVisibility="Hidden" Grid.Row="0" />

        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Button Style="{StaticResource RoundedButtonStyle}" x:Name="BackButton" Height="40" Width="60" HorizontalAlignment="Center" VerticalAlignment="Center" Content="Back" Click="BackButton_Click" Grid.Column="0"/>
            <Button Style="{StaticResource RoundedButtonStyle}" x:Name="NextButton" Height="40" Width="60" HorizontalAlignment="Center"  VerticalAlignment="Center" Content="Next" Click="NextButton_Click" Grid.Column="1"/>
        </Grid>
    </Grid>
</Page>
